//1.Reverse an array? Input: [1, 2, 3, 4, 5, 6]
//4.Merge to sets in javascript? (hint use Set class in javascript)

const arrayReverse = (array)=>{
array.reverse();
return array;
}

const array1 = [1, 2, 3, 4, 5, 6]
const reversedArray =  arrayReverse(array1);
console.log(array1);